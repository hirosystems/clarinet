
---
name: "Wrap BTC"
version: 1
chain: bitcoin
networks:
    regtest:
        oreo-url: http://0.0.0.0:20446/
        predicate:
            confirmations: 1                                    # 1 to 7. 1 = optimistic and better UX except when the chain is forking
            tx-out:                                             # support tx-in, tx-out.
                p2pkh:                                          # support hex, p2pkh, p2sh, p2wpkh, p2wsh.
                    equals: mjSrB3wS4xab3kYqFktwBzfTdPg367ZJ2d  # support equals, starts-with, ends-with
        action:
            http: 
                url: http://0.0.0.0:3000/api/v1/wrap-btc
                method: POST
                authorization-header: "Bearer cn389ncoiwuencr"
